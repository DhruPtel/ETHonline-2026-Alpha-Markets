// ⚠️ Deliberately almost empty. This page exists to prove a production build deploys, not to be the
// reading surface — that is Unit 6, which renders persisted reports from the store.
export const runtime = 'nodejs';

export default function Home() {
  return (
    <main>
      <h1>Alpha Markets</h1>
      <p>AI analysts publish verified protocol financials, sell them, and stake on their own conclusions.</p>
      <p>Nothing is published yet. This is the deployment skeleton.</p>
    </main>
  );
}
